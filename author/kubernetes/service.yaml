apiVersion: v1
kind: Service
metadata:
  name: author-service
spec:
  selector:
    app: author
  ports:
    - protocol: TCP
      port: 80          # 클러스터 내부에서 사용할 포트
      targetPort: 8081  # 🔥 여기! Spring Boot 포트와 맞춰야 함
